import 'package:flutter_test/flutter_test.dart';

void main() {
  test('CI Placeholder - El sistema compila correctamente', () {
    // Este test siempre pasa. Sirve para verificar que el CI funciona.
    expect(true, true);
  });
}
